/**
 * No front-end behavior needed for this block -- it's a pure layout
 * wrapper; its actual children (gateway/pagination) handle their own
 * interactivity independently. This file exists solely so the build picks
 * it up as an entry and extracts style.scss into build/style-view.css,
 * matching block.json's "style" field path -- block.json deliberately
 * declares no "viewScript" for this block, since there'd be nothing for
 * one to do.
 */
import './style.scss';
