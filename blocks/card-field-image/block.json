{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "gateway/card-field-image",
	"title": "Field: Image",
	"category": "widgets",
	"icon": "format-image",
	"description": "Displays one Image field's value from the current record as a real image, honoring the field's own configured Return Format (Array/URL/ID) and, where possible, a chosen Size. Only meaningful inside a Collection-sourced Data Cards card template, a Related Items loop, a Data Display detail template, or a Single Record template.",
	"keywords": [ "field", "image", "photo", "picture", "media", "model", "collection", "data cards" ],
	"version": "0.1.0",
	"textdomain": "gateway",
	"ancestor": [ "gateway/data-cards-body", "gateway/data-display", "gateway/single-record" ],
	"usesContext": [
		"gateway/data-cards/sourceType",
		"gateway/data-cards/collection",
		"record"
	],
	"attributes": {
		"fieldKey": {
			"type": "string",
			"default": ""
		},
		"size": {
			"type": "string",
			"default": "full"
		}
	},
	"supports": {
		"html": false
	},
	"editorScript": "file:./build/index.js",
	"render": "file:./render.php"
}
