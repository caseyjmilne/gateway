{
	"name": "gateway/gateway",
	"description": "Vendored PHP dependencies for the Gateway WordPress plugin -- committed to the repo (see vendor/README.md) so a site installing this plugin never needs to run Composer itself.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=8.2",
		"illuminate/database": "^11.0",
		"league/commonmark": "^2.10"
	},
	"config": {
		"platform": {
			"php": "8.2"
		},
		"sort-packages": true,
		"optimize-autoloader": true
	},
	"minimum-stability": "stable"
}
